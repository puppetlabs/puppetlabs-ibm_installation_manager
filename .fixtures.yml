fixtures:
  repositories:
    datetime:
      repo: 'git://github.com/fervidus/datetime.git'
      ref: 'master'
    stdlib:
      repo: 'git://github.com/puppetlabs/puppetlabs-stdlib.git'
      ref: '4.6.0'
    archive:
      repo: 'git://github.com/voxpupuli/puppet-archive.git'
      ref: 'master'
  symlinks:
    'ibm_installation_manager': "#{source_dir}"
